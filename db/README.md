# Estructura de la base de datos
Se usa [MongoDB](https://www.mongodb.com/es), con una base de datos `jose` y dos colecciones, `sightings` y `astronomical-events`.


## Despliegue (mongo-express)
Levantar en <localhost:8081>:

```
docker compose up mongo-express
```